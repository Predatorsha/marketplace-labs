# Portable defaults. Relative paths resolve to the project root in dev,
# and to Electron userData when packaged (see resolveAppPath).
# Optional local override (gitignored): config.local.yaml
output:
  market_root: "data"
  catalog_db: "data/catalog.sqlite"

browser:
  market_profile_dir: ".browser-profile"
